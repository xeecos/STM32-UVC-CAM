#pragma once
// #include <Arduino.h>
#include "serial.h"

// HardwareSerial uart(PA10, PA9);

void serial_init(void) {
    // uart.begin(115200);
}
void uart_log(const char*msg)
{
    // uart.println(msg);
}